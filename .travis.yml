matrix:
  include:
    - language: python
    script:
      - pytest
    -language: node_js
    before_install:
      - nvm install 6
      - node --version
      - npm --version
    install:
      - npm install -g serverless
    - language: java
